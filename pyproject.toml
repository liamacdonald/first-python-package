[build-system]
requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "first_py_package"
dynamic = ["version"]

[tool.setuptools_scm]
write_to = "first_py_package/_version.py"